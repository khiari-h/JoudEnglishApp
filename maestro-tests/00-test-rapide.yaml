# Test E2E : Test rapide de vérification
# Vérifie que Maestro fonctionne et peut lancer l'app

appId: com.hamdanek.JoudEnglishApp
name: "Test rapide - Vérification Maestro"
tags:
  - "verification"
  - "quick-test"

env:
  TIMEOUT: 10000
  RETRY_COUNT: 1

# Test simple de vérification
---
- openLink: "exp+joudenglishapp://expo-development-client/?url=http://localhost:8082"
- assertVisible:
    id: "hero-empty-section"
- assertVisible:
    id: "level-selection-button"

// src/components/screens/exercises/reading/ReadingNavigation/style.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: 'white',
    borderTopWidth: 1,
    borderTopColor: '#f1f5f9',
    padding: 0, // Le padding est géré par NavigationButtons
  },
});

export default styles;
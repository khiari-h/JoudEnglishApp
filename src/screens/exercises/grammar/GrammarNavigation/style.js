// src/components/screens/exercises/grammar/GrammarNavigation/style.js
import { StyleSheet } from 'react-native';

/**
 * 🎯 Styles Ultra-Simples pour GrammarNavigation
 * - Réutilise NavigationButtons donc minimal styling needed
 * - Cohérent avec VocabularyNavigation
 * - Pas de duplication de code
 */
const styles = StyleSheet.create({
  container: {
    // Le style est géré par NavigationButtons lui-même
    // Pas besoin de recréer du glassmorphism ici
  },
});

export default styles;